#include "../../include/core/matching.h"

qty_t match_order(order_book_t *book, order_t *incoming, trade_t *trades,
                  size_t max_trades) {
  (void)book;
  (void)incoming;
  (void)trades;
  (void)max_trades;
  return 0;
}
